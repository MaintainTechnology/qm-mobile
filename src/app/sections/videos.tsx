// Dashboard section route — pushed from the Menu tab's grouped nav.
import { VideosScreen } from '@/features/sections/VideosScreen';

export default function VideosRoute() {
  return <VideosScreen />;
}
