// Dashboard section route — pushed from the Menu tab's grouped nav.
import { InvitesScreen } from '@/features/sections/InvitesScreen';

export default function InvitesRoute() {
  return <InvitesScreen />;
}
