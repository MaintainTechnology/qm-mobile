// Dashboard section route — pushed from the Menu tab's grouped nav.
import { BillingScreen } from '@/features/sections/BillingScreen';

export default function BillingRoute() {
  return <BillingScreen />;
}
