// Dashboard section route — pushed from the Menu tab's grouped nav.
import { PricingBookScreen } from '@/features/sections/PricingBookScreen';

export default function PricingBookRoute() {
  return <PricingBookScreen />;
}
