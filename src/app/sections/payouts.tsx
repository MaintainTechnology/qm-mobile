// Dashboard section route — pushed from the Menu tab's grouped nav.
import { PayoutsScreen } from '@/features/sections/PayoutsScreen';

export default function PayoutsRoute() {
  return <PayoutsScreen />;
}
