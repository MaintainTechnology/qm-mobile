// Dashboard section route — pushed from the Menu tab's grouped nav.
import { FilesScreen } from '@/features/sections/FilesScreen';

export default function FilesRoute() {
  return <FilesScreen />;
}
