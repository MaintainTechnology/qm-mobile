import { PlaceholderScreen } from '@/features/shell/PlaceholderScreen';

export default function ChatsRoute() {
  return (
    <PlaceholderScreen
      title="Chats"
      body="Customer conversations your AI is handling will live here. This screen is next in the build queue."
    />
  );
}
